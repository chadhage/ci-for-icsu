{
  "name": "smart-ci-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Validation and tests for the Smart CI static learning experience.",
  "scripts": {
    "validate": "node tools/validate-site.js",
    "test": "node --test tests/*.test.js",
    "check": "npm run validate && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}